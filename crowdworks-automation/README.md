# クラウドワークス案件応募 半自動化ツール

新着案件を定期的にチェックし、AIが応募文のドラフトを作成してレポートにまとめるツールです。
**送信は自動化しません**（講師の助言どおり半自動）。レポートを見て内容を確認・修正してから、自分でクラウドワークス上で応募してください。

## できること

1. 指定した検索条件（キーワード・カテゴリなど）で新着案件を取得
2. すでに見た案件は除外（重複通知なし）
3. 予算条件（`minBudget`）でフィルタ（案件ページのテキストから金額を推定するため、あくまで目安）
4. 案件内容 + あなたの応募定型文をもとに、AIが応募文ドラフトを作成
5. `data/reports/` にMarkdownレポートを出力

## セットアップ

```bash
cd crowdworks-automation
pnpm install
cp .env.example .env
cp search-conditions.example.json search-conditions.json
cp applicant-template.example.md applicant-template.md
```

1. `.env` にクラウドワークスのログイン情報と Anthropic API キーを入力
2. `search-conditions.json` に、クラウドワークスで実際に検索条件を設定した後の**検索結果ページURL**を貼り付け
   （例: キーワードやカテゴリで絞り込んだ状態のURLをブラウザからコピー）
3. `applicant-template.md` に、応募文に必ず入れたい内容（強み・ポートフォリオ・稼働条件など）を記入

## 実行

```bash
# 通常実行（バックグラウンドでブラウザ操作）
pnpm run

# 初回は画面を見ながら動作確認することを推奨
pnpm run:headful
```

## 定期実行（1日3回など）

MVPではスケジューラを内蔵していません。macOSの `launchd` や `cron` から
`pnpm run` を呼び出す形で、9時・12時・15時などに実行してください。
（動かし方は次のステップで一緒に設定します）

## 既知の制約・要確認事項

- `src/scraper.ts` 内のCSSセレクタはCrowdWorksの現行UIをもとにした推測値です。
  実際のログイン・案件一覧取得で動作しない場合は、`pnpm run:headful` で
  ブラウザの挙動を見ながら調整が必要です。
- 予算フィルタ（`minBudget`）は案件ページのテキストから数値を推定しているだけなので、
  誤って除外/通過するケースがあります。最終判断はレポートを見て自分で行ってください。
- クラウドワークス利用規約 第23条12号により、サーバーに過剰な負荷をかける行為は禁止されています。
  検索条件（`searchUrl`）の数や実行頻度を過度に増やさないでください（1日数回・数件の検索条件を想定）。
